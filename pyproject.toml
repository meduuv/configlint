[build-system]
requires=["setuptools>=69"]
build-backend="setuptools.build_meta"
[project]
name="configlint"
version="0.1.0"
description="Configuration linter"
requires-python=">=3.10"
[project.scripts]
configlint="configlint.cli:main"
[tool.setuptools.packages.find]
where=["src"]
